# ⚠️  DEPRECATED — This repo uses pyproject.toml + Hatch as the source of truth.
# Prefer: pip install -e ".[dev]"  (or: hatch shell)
# This file exists only for compatibility with tools that require it.
# Development dependencies for simple-python-boilerplate
# Install with: python -m pip install -r requirements-dev.txt
#
# NOTE: This file mirrors [project.optional-dependencies].dev in pyproject.toml
# For editable install with dev deps, prefer: python -m pip install -e ".[dev]"
# This file is primarily for users who prefer using pip and requirements files.

# Include runtime dependencies
-r requirements.txt

# Testing
pytest  # Simple powerful testing with Python (v9.0.2)
pytest-cov  # Pytest plugin for measuring coverage (v7.0.0)

# Linting & Formatting
ruff  # An extremely fast Python linter and code formatter, written in Rust (v0.15.0)

# Type Checking
mypy  # Optional static typing for Python (v1.19.1)

# Security
bandit  # Security oriented static analyser for python code (v1.9.3)

# Git Hooks
pre-commit  # A framework for managing and maintaining multi-language pre-commit hooks (v4.5.1)

# Optional helpful tools (uncomment as needed)
# pipdeptree      # Dependency tree visualization
# pip-tools       # Pin dependencies with pip-compile
# pip-audit       # Security vulnerability checker
# rich            # Pretty console output
# icecream        # Better debug printing
# ipython         # Enhanced interactive shell
