# Development dependencies for simple-python-boilerplate
#
# This file is OPTIONAL. The source of truth for dependencies is
# pyproject.toml, managed via Hatch (https://hatch.pypa.io/).
# Preferred install methods:
#   pip install -e ".[dev]"   (editable install with dev deps)
#   hatch shell               (enter Hatch-managed dev environment)
#
# This file exists for tools and workflows that require a requirements file
# (e.g., pip-audit, Dependabot, Docker builds without Hatch).
# Keep it in sync with [project.optional-dependencies].dev in pyproject.toml.
#
# TODO (template users): When you add or remove dev dependencies in
#   pyproject.toml, update this file to match. If you only use Hatch
#   and never need a plain requirements file, you can delete this file.

# Include runtime dependencies
-r requirements.txt

# Testing
pytest>=8.0  # Simple powerful testing with Python (v9.0.2)
pytest-cov>=6.0  # Pytest plugin for measuring coverage (v7.0.0)

# Linting & Formatting
ruff>=0.11  # An extremely fast Python linter and code formatter, written in Rust (v0.15.1)

# Type Checking
mypy>=1.15  # Optional static typing for Python (v1.19.1)

# Security
bandit>=1.8  # Security oriented static analyser for python code (v1.9.3)

# Git Hooks
pre-commit>=4.0  # A framework for managing and maintaining multi-language pre-commit hooks (v4.5.1)

# Commit & Dependency Hygiene
commitizen>=4.4  # Conventional commit helper + validator (v4.13.8)
deptry>=0.22  # Detect unused, missing, and transitive dependencies (v0.24.0)

# Optional helpful tools (uncomment as needed)
# pipdeptree      # Dependency tree visualization
# pip-tools       # Pin dependencies with pip-compile
# pip-audit       # Security vulnerability checker
# rich            # Pretty console output
# icecream        # Better debug printing
# ipython         # Enhanced interactive shell
