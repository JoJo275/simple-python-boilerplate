# Known Issues & Tech Debt

Acknowledged gaps and limitations in this template project. Maintained
so they don't need to be rediscovered each session. Remove entries as
they're resolved; add new ones as they're found.

<!-- TODO (template users): After forking, delete template-specific entries
     (marked "Template") and seed this file with your own project's known
     issues. Keep it honest — hiding tech debt doesn't make it go away. -->

---

## Active Issues

| Area         | Issue                              | Details                                                                                                                                                                    | Severity |
| :----------- | :--------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------- |
| **CI**       | Container scan is advisory-only    | `container-scan.yml` results don't block PRs. Intentional (high false-positive rate), but means container vulnerabilities can ship unnoticed.                               | Low      |
| **CI**       | Docs deploy is path-filtered       | `docs-deploy.yml` only runs on `docs/`, `src/`, `mkdocs.yml` changes pushed to main. Build _validation_ is covered by `docs-build.yml` (runs every PR, in CI gate).       | Info     |
| **CI**       | Workflow guards use `YOURNAME`     | Workflow `if:` guards reference `YOURNAME/YOURREPO` — intentional per [ADR 011](adr/011-repository-guard-pattern.md) (disabled by default for forks). Enable via repo vars. | Info     |
| **CI**       | CI gate check-name drift risk      | If a workflow job's `name:` field is renamed without updating `REQUIRED_CHECKS` in `ci-gate.yml`, the gate silently stops finding it and times out.                        | Medium   |
| **Docs**     | `docs/workflows.md` can drift      | Manually maintained workflow inventory. Adding/removing workflows without updating this file makes it misleading.                                                           | Medium   |
| **Docs**     | `docs/reference/commands.md` scope | Auto-generated by the MkDocs hook, but only covers Taskfile tasks, script `--help` output, and Hatch envs. Custom entry points or Makefile targets aren't included.        | Low      |
| **Template** | Placeholder source code            | Everything under `src/simple_python_boilerplate/` is example code. Template users must replace it entirely.                                                                | Info     |
| **Security** | PGP key fingerprint is a TODO      | `SECURITY.md` has contact email but PGP key fingerprint is still a placeholder.                                                                                            | Low      |
| **Hatch**    | Stale env after dependency removal | After removing a dependency from `pyproject.toml`, the old package silently remains until `hatch env remove default` is run. Hatch doesn't auto-uninstall.                 | Medium   |
| **Scripts**  | `_SKIP_SCRIPTS` is hardcoded       | `generate_command_reference.py` has a hardcoded skip list. Should be configurable via `pyproject.toml` or a config file.                                                   | Low      |

## Resolved

Move resolved issues here with a brief note on how/when they were fixed,
then delete them after a release cycle.

| Area | Issue | Resolution | Date |
| :--- | :---- | :--------- | :--- |
|      |       |            |      |

## How to Use This File

- **Found something?** Add a row to the Active table with area, description,
  and severity (Info / Low / Medium / High).
- **Fixed something?** Move it to Resolved with a date and brief note.
- **Copilot context:** This file is referenced from
  `.github/copilot-instructions.md` so Copilot knows about current gaps
  without the instructions file growing unbounded.
