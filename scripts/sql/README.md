# Ad-hoc SQL Scripts

Scratch space for SQL experimentation and one-off scripts.

## Usage

```bash
# Run a script
sqlite3 var/app.sqlite3 < scripts/sql/scratch.sql

# Interactive mode
sqlite3 var/app.sqlite3
```

**Note:** These are not version-controlled queries—use `db/queries/` for reusable SQL.
