# Database Seeds

Seed files for populating the database with initial or test data.

## Naming Convention

```
001_seed_users.sql
002_seed_categories.sql
```

## Usage

```bash
# Apply seeds (example)
sqlite3 var/app.sqlite3 < db/seeds/001_seed_users.sql
```
